name: Publish npm package

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: read

jobs:
  build:
    name: Build (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: true
      matrix:
        include:
          - os: ubuntu-latest
            target: x86_64-unknown-linux-gnu
            artifact: linux-x64
          - os: ubuntu-latest
            target: aarch64-unknown-linux-gnu
            artifact: linux-arm64
          - os: macos-latest
            target: aarch64-apple-darwin
            artifact: darwin-arm64

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Install cross-compilation tools (Linux ARM64)
        if: matrix.target == 'aarch64-unknown-linux-gnu'
        run: |
          sudo apt-get update
          sudo apt-get install -y gcc-aarch64-linux-gnu

      - name: Rust cache
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: |
            . -> target

      - name: Build N-API addon (release)
        run: cargo build -p htsvcf-napi --release --target ${{ matrix.target }}
        env:
          CARGO_TARGET_AARCH64_UNKNOWN_LINUX_GNU_LINKER: aarch64-linux-gnu-gcc

      - name: Copy addon
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p artifacts
          if [[ "${{ runner.os }}" == "Linux" ]]; then
            cp -f target/${{ matrix.target }}/release/libhtsvcf_napi.so artifacts/htsvcf.${{ matrix.artifact }}.node
          elif [[ "${{ runner.os }}" == "macOS" ]]; then
            cp -f target/${{ matrix.target }}/release/libhtsvcf_napi.dylib artifacts/htsvcf.${{ matrix.artifact }}.node
          fi

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: bindings-${{ matrix.artifact }}
          path: artifacts/htsvcf.${{ matrix.artifact }}.node
          if-no-files-found: error

  publish:
    name: Publish to npm
    runs-on: ubuntu-latest
    needs: build
    permissions:
      contents: read
      id-token: write
    # Required for OIDC trusted publishing
    environment: npm

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '24'
          registry-url: 'https://registry.npmjs.org'

      - name: Download Linux x64 artifact
        uses: actions/download-artifact@v4
        with:
          name: bindings-linux-x64
          path: npm/htsvcf/

      - name: Download Linux arm64 artifact
        uses: actions/download-artifact@v4
        with:
          name: bindings-linux-arm64
          path: npm/htsvcf/

      - name: Download macOS arm64 artifact
        uses: actions/download-artifact@v4
        with:
          name: bindings-darwin-arm64
          path: npm/htsvcf/

      - name: Extract version from tag
        id: version
        run: echo "VERSION=${GITHUB_REF#refs/tags/v}" >> $GITHUB_OUTPUT

      - name: Update package.json version
        run: |
          cd npm/htsvcf
          npm version ${{ steps.version.outputs.VERSION }} --no-git-tag-version

      - name: Publish to npm
        run: |
            cd npm/htsvcf 
            # npm run build --if-present
            # npm install
            # npm ci
            npm publish --provenance --access public
